<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Register - TO DO LIST APP</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen flex items-center justify-center bg-gradient-to-r from-blue-200 to-blue-400">
    <div class="w-full max-w-xl bg-white rounded-lg shadow-lg p-8">
        <h2 class="text-2xl font-bold mb-6 text-center">Create a new account</h2>
        <?php if (!empty($error)): ?>
            <div class="mb-4 p-2 bg-red-200 text-red-800 rounded">
                <?php echo $error; ?>
            </div>
        <?php endif; ?>
        <form method="POST" action="<?php echo $this->baseUrl(); ?>/register" class="space-y-4">
            <div>
                <label class="block font-semibold mb-1" for="name">Name</label>
                <input type="text" id="name" name="name" class="w-full border-2 border-blue-400 rounded px-3 py-2" required>
            </div>
            <div>
                <label class="block font-semibold mb-1" for="surname">Surname</label>
                <input type="text" id="surname" name="surname" class="w-full border-2 border-blue-400 rounded px-3 py-2" required>
            </div>
            <div>
                <label class="block font-semibold mb-1" for="date_of_birth">Date of Birth</label>
                <input type="date" id="date_of_birth" name="date_of_birth" class="w-full border-2 border-blue-400 rounded px-3 py-2" required>
            </div>
            <div>
                <label class="block font-semibold mb-1" for="email">Email</label>
                <input type="email" id="email" name="email" class="w-full border-2 border-blue-400 rounded px-3 py-2" required>
            </div>
            <div>
                <label class="block font-semibold mb-1" for="password">Password</label>
                <input type="password" id="password" name="password" class="w-full border-2 border-blue-400 rounded px-3 py-2" required>
            </div>
            <div>
                <label class="block font-semibold mb-1" for="confirm_password">Confirm Password</label>
                <input type="password" id="confirm_password" name="confirm_password" class="w-full border-2 border-blue-400 rounded px-3 py-2" required>
            </div>
            <div class="flex justify-end">
                <button type="submit" class="bg-blue-400 hover:bg-blue-500 text-white font-semibold px-8 py-2 rounded">Register</button>
            </div>
            <p class="text-center">
                Already have an account? <a href="<?php echo $this->baseUrl(); ?>/login" class="text-blue-600 underline">Login</a>
            </p>
        </form>
    </div>
</body>
</html>