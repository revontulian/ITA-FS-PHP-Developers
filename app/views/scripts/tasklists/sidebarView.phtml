<!-- Example bullet-point list -->
<ul class="list-disc list-inside mt-6 ml-6 text-black">
    <?php if (!isset($tasklists) || empty($tasklists)): ?>
        <li class="text-gray-500">No tasklists available.</li>
    <?php else: ?>
        <?php foreach ($tasklists as $tasklist): ?>
            <li class="flex items-center justify-between pr-2">
                <span>
                    <a
                        href="<?= $this->baseUrl() ?>/tasklists/select?id=<?= urlencode($tasklist['id']) ?>"
                        class="hover:font-bold <?= (isset($_SESSION['tasklistId']) && $_SESSION['tasklistId'] == $tasklist['id']) ? 'font-bold' : '' ?>"
                        title="Select tasklist">
                        <?= htmlspecialchars($tasklist['name']) ?>
                    </a>
                </span>
                <span class="flex space-x-2 ml-2">
                    <!-- Edit icon -->
                    <a href="<?= $this->baseUrl() ?>/tasklists/edit?id=<?= urlencode($tasklist['id']) ?>" title="Edit">
                        <svg class="w-4 h-4 text-blue-500 hover:text-blue-700" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                            <path d="M15.232 5.232l3.536 3.536M9 13l6.586-6.586a2 2 0 112.828 2.828L11.828 15.828a2 2 0 01-2.828 0L9 13z"></path>
                            <path d="M16 7l-1.5-1.5"></path>
                        </svg>
                    </a>
                    <!-- Delete icon -->
                    <a href="<?= $this->baseUrl() ?>/tasklists/delete?id=<?= urlencode($tasklist['id']) ?>" title="Delete" onclick="return confirm('Are you sure you want to delete this tasklist?')">
                        <svg class="w-4 h-4 text-red-500 hover:text-red-700" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
                        </svg>
                    </a>
                </span>
            </li>
        <?php endforeach; ?>
    <?php endif; ?>
</ul>
<!-- End bullet-point list -->

<!-- Tasklist Creation Section -->
<form action="<?= $this->baseUrl() ?>/tasklists/create" method="post" class="mt-4 flex items-center ml-6 space-x-2">
    <input
        type="text"
        name="tasklist_name"
        placeholder="Enter new tasklist name"
        class="border border-gray-300 rounded px-3 py-1 focus:outline-none focus:ring-2 focus:ring-blue-200"
        required>
    <button
        type="submit"
        class="bg-blue-500 hover:bg-blue-700 text-white font-semibold px-4 py-1 rounded">
        <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" d="M12 4v16m8-8H4" />
        </svg>
    </button>
</form>