<div class="w-full max-w-xl bg-white rounded-lg shadow-lg p-8">
    <h2 class="text-2xl font-bold mb-6 text-center">Create a new tasklist</h2>

    <form method="POST" action="<?php echo $this->baseUrl(); ?>/tasklists/create" class="space-y-4">
        <div class="form-group">
            <label class="block font-semibold mb-1" for="name">Tasklist Name:</label>
            <input type="text" id="name" name="name" class="w-full border-2 border-blue-400 rounded px-3 py-2" required>
        </div>

        <div class="form-group">
            <button type="submit">Create Tasklist</button>
        </div>
    </form>

    <div class="form-group">
        <a href="/tasklists">Back to Tasklists</a>
    </div>

    <?php if (isset($error)): ?>
        <div class="alert alert-danger">
            <?= htmlspecialchars($error) ?>
        </div>
    <?php endif; ?>

    <?php if (isset($success)): ?>
        <div class="alert alert-success">
            <?= htmlspecialchars($success) ?>
        </div>
    <?php endif; ?>
</div>